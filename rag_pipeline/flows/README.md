# Flows

`flows/` is for high-level runnable workflows that stitch together lower-level modules from `agents/`, `search/`, `ingest/`, and `tools/`.

Current canonical flow:

- `report/`: full report generation, evidence extraction, reformatting, and review.

Existing top-level scripts are intentionally kept as compatibility entry points.

